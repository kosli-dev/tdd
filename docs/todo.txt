
# Core
Style the submit button
Print overall % of coverage on terminal
Add data to ScoreForm - allow number of squads to be dynamic
Add a few system API tests
Add a few system APP tests

# Maybe?
Make t fixture so its available to both system and unit tests
Drop Swagger RestX
Document API call
Investigate calling coverage.embed.cleanup() so there is no need to kill server
  https://github.com/pytest-dev/pytest-cov/blob/master/src/pytest_cov/embed.py

# Marketing
Write slides. Present.
Write blog posts
