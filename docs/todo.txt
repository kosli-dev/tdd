
# Core
Add validators to ensure form is only submitted when 5 letters are entered
Add simple .github workflow to run unit and system tests
Reinstate read_only file-system and volume-mounts in docker-compose.yaml
Create CSS/JS files in dockerfile multi-stage build (like server)
Run unit tests in parallel and run coverage combine
Add some simple system APP tests

# Maybe?
Drop Bundling of JS
Collect coverage of system tests themselves
   Will require coverage combine on the .coverage files from _two_ containers
Add separate env-files for unit/system tests
Make t fixture available to both system and unit tests
Document API call

