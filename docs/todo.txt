
# Core
Get basic html GET -> POST working, render/redirect
Translate all Ruby unit tests to passing Python tests
Add a few API tests
Add a few APP tests
Add jQuery js file and make the number of Squads dynamic.

# Maybe
Generate coverage stats in browser whether tests pass or not?
Make t fixture so its available to both system and unit tests
Drop Swagger? RestX
Document API call
Investigate calling coverage.embed.cleanup() so there is no need to kill server?
  https://github.com/pytest-dev/pytest-cov/blob/master/src/pytest_cov/embed.py

# Marketing
Write slides
Write blog posts
