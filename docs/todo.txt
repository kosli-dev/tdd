
# Core
Print overall % of coverage
Add submit button - get basic html GET -> POST working, render/redirect
Add a few API tests
Add a few APP tests
Add JS to make the number of Squads dynamic.

# Maybe?
Make t fixture so its available to both system and unit tests
Drop Swagger RestX
Document API call
Investigate calling coverage.embed.cleanup() so there is no need to kill server
  https://github.com/pytest-dev/pytest-cov/blob/master/src/pytest_cov/embed.py

# Marketing
Write slides. Present.
Write blog posts
